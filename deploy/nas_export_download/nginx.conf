server {
    listen 8788;
    server_name _;
    client_max_body_size 1m;
    auth_basic "Expert Pack NAS";
    auth_basic_user_file /run/secrets/download.htpasswd;

    location = /healthz {
        auth_basic off;
        proxy_pass http://download-api:8080/healthz;
    }

    # Signed download URLs are authenticated by the API's HMAC token. Requiring
    # Basic Auth here would prevent browser download links from ever reaching it.
    location /download/ {
        auth_basic off;
        proxy_pass http://download-api:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    location / {
        proxy_pass http://download-api:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    location /_protected/ {
        # This internal redirect is only emitted after the API validates the
        # signed token. It must not inherit the operator-facing Basic Auth gate.
        auth_basic off;
        internal;
        alias /exports/accepted/;
        sendfile on;
        tcp_nopush on;
        add_header Accept-Ranges bytes always;
        add_header X-Content-Type-Options nosniff always;
    }
}
