# 新中台首版部署约定

起点为清理完成后的 `ed576ff7bb9557df6b2bbd66eaf988bdcd531379`。先完成 NAS 完整归档和云端冷数据回收，再部署本版本。旧 Web 和旧业务生产者不恢复。

## 本版交付

1. 四币（BTC/ETH/SOL/BNB）、4h / 24h 的结构化交易参考，包含明确动作、原因、来源、成本、样本范围与失效时间。
2. NAS 上受限的单次分析任务：历史统计、当前参考、不可变前向预测记录及到期结果。任务完成即退出，不部署常驻研究/导出/AI 三套 worker。
3. qyun2 上的紧凑输入发布、结果校验和只读 GET；在线请求只读紧凑结果，不扫描历史湖、不同步等待 NAS。
4. 一个静态 Web 工作界面，主导航为“交易参考 / 效果观察 / 实验与数据”，复用现有 FastAPI，不增加 Node 服务或 Streamlit。

本次 V5 消费边界是可读取的研究参考接口；不改变 V5 策略、仓位或订单控制。页面必须说明实际读取/采纳证据，未接入的账户级 A/B 不得显示为已完成的收益对照。

## 分析语义

- 仅使用闭合 K 线，时间统一 UTC。历史相似行情是描述统计，不称为已验证的交易胜率。
- 首个实验冻结为 1H 决策窗口、24 小时趋势条件、4h/24h 观察时域；不进行大规模参数搜索。
- 历史参考使用明确的一根 K 线决策延迟，排除缺口、重复事件和跨边界未成熟标签；不同 horizon 是同一机会的观察维度。
- 使用当前全程往返成本做压力换算时，明确称为当前成本假设下的历史结果，不冒充逐笔历史真实成本。
- 结构化动作使用 `DEFER / REVIEW_ENTRY / KEEP_BASELINE / NO_VIEW`。全部保持 `research_only` 与 `live_order_effect=none`，不能提升风险许可。
- 过期、字段缺失、结果签名错误、时间异常、样本不足时明确输出原因；不续期旧建议、不填假的概率和收益。
- 前向预测从真实发布时点开始，不回填历史预测。每个 symbol / signal time / horizon / frozen version 只登记一次；评价窗口开始前未发布的预测不能纳入前向成绩。
- 前向资产收益标签与 V5 同资金账户增益是不同指标。首版账户级增益为空，并注明尚无基线对照证据。

## 数据和资源

- 完整归档仍在 NAS HDD `/volume2/quant-lab/archive/retirement-20260905`，新工作目录独立创建，不修改封存原件。
- NAS 分析默认并发 1，最多 3 CPU、4 GiB RAM，不使用 swap 扩张工作集；暂存和结果设置容量上限，历史大文件留 HDD。
- qyun2 新任务目标 1 GiB 内、与现有重任务锁协调，输入输出均按不可变版本/内容哈希组织。
- 使用现有签名实现与原子发布机制；NAS 断网时保留待处理任务，云端到期输出无观点，不将重计算搬回云端。

## Web 设计

- 视觉方向：与 V5 当前控制台统一，石墨黑底、琥珀金强调、细分隔线与等宽数字。主内容铺满剩余窗口宽度；基础字号随大屏从 16px 增大至 22px，手机保持 16px；窄屏表格可横向滚动。
- 内容层级：默认展示当前四币的参考动作与时效；选择币种后查看依据和收益分布；前向效果与数据状态放在各自工作页。
- 交互：币种选择与详情平滑切换、导航焦点与刷新状态明确；顶部提供进入/退出全屏，浏览器拒绝时显式提示；尊重减少动态效果设置。
- 网页无需访问密钥，直接读取公开的 `GET /v1/trade-advice/latest` 与规范建议 ID 的详情 GET。签名、时效与数据完整性校验继续执行；其余策略 API 与显式 IP 限制保留现有认证规则，匿名写入不开放。网页移除登录表单，并清除旧会话保存的访问密钥。

## 验收

- 针对时间穿越、未闭合 K 线、缺口、重复、成本重复扣减、TTL、签名、重放和样本不足建立测试。
- 全量 pytest 与 Ruff；真实 NAS 限额任务完成，记录运行时间与峰值内存；真实 qyun2 GET 仅读取紧凑文件。
- 真实浏览器验证大屏与窄屏布局、无密钥直达、全屏、筛选、详情、过期/无结果显示；不用演示数据冒充生产结果。
- 推送代码、部署 NAS 与 qyun2、核对版本和实际建议，再交付访问入口、变化、风险和回滚点。

## 运行与数据路径

| 对象 | 路径与机制 |
| --- | --- |
| 云端任务 | `quant-lab-decision.timer` 每五分钟，`qlab decision cloud-cycle`；共用 heavy lock，1 GiB / 50% CPU / 150 秒 |
| 云端传输 | `/var/lib/quant-lab/decision/{inputs,inbox,results,receipts}`；独立 `quant-decision` 组；NAS 只能写 inbox |
| API 发布 | `/var/lib/quant-lab/lake/gold/decision_reference/publication.json` 为原子提交点；读失败显式 503，过期视图变为 NO_VIEW |
| NAS 任务 | `deploy/decision/run_nas_decision.sh`，单锁，12 分钟超时，3 CPU / 4 GiB / 128 PID / 256 MiB 临时盘，运行后退出 |
| NAS 历史 | `/volume2/quant-lab/decision/archive/{inputs,history,corrections,results}` 与单写者 `forward.duckdb`；不自动删除原件 |
| 传输安全 | 复用现有 `quant-research` SSH 账号和固定 known_hosts；输入与发布回执由云端 Ed25519 签名，结果与归档回执由 NAS 独立密钥签名 |
| 热数据回收 | 云端保留七天；只回收匹配 NAS 已签名逐文件 SHA256 读回回执的原件，当前结果和引用保留；原始数据与完整结果留 HDD |

没有及时发布的建议不登记为前向观察。标签必须等参考退出小时线闭合并实际到达，完整窗口缺一根线就保留“缺少行情”；已封存标签不因后续价格修订而重算。4h 与 24h 按动作分别统计，不汇总成混合账户收益。相同输入、相同观察状态重复运行，不新增结果，不续期原建议。

## 部署与回滚

分析代码版本同时固定在云端 `runtime.env` 与 NAS 镜像 `quant-decision:<40位提交>`。两端分析版本不一致时拒绝结果；仅 Web / API 展示或认证变更可以单独部署 qyun2，保留已经匹配的两端分析版本。私钥只放 `/etc/quant-lab/decision` 与 NAS `decision/private`，不进入仓库、镜像或 Web。

启用前先生成两端专用签名密钥并交换公钥，建立 setgid 传输目录，配置固定 SSH known_hosts，运行一次 cloud-cycle，再运行 NAS 单次任务、再次 cloud-cycle 完成签名发布。只有实际链路通过后才启用定时任务。

回滚新版本时停用新增 decision timer 和 NAS 新任务，恢复精简底座提交 `ed576ff7bb9557df6b2bbd66eaf988bdcd531379`，重启 API；保留新输入、历史和结果供检查。HTTPS 代理可继续提供底座入口，或单独停用。无需启动已退役模块或覆盖湖数据。恢复新版本时必须同时固定两端代码与匹配公钥，禁止用旧数据伪造新发布时间。
